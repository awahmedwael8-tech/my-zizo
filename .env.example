# Copy this file to `.env` and fill in real values. `.env` is gitignored.
# Get your DEV secret key from the Trigger.dev dashboard → API Keys.
TRIGGER_SECRET_KEY=tr_dev_xxxxxxxxxx

# Only needed for a self-hosted Trigger.dev instance:
# TRIGGER_API_URL=https://your-self-hosted-instance.com
